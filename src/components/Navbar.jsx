import { Languages, LayoutDashboard, LogOut, Moon, Radio, Sun } from 'lucide-react'
import SearchBar from './SearchBar'
import logo from '../assets/aapatsuchana-logo.svg'
import '../navbar.css'
export default function Navbar({ onSearch, onSignOut, onEmployeeDashboard, theme, onToggleTheme, language, onToggleLanguage }) { const nepali = language === 'ne'; return <header className="topbar"><div className="brand"><span className="brand-mark"><img className="dashboard-logo" src={logo} alt="" /></span><div><strong>AapatSuchana</strong><span>{nepali ? 'विपद् तथा जोखिम नक्सा' : 'DISASTER & HAZARD MAP'}</span></div></div><SearchBar onSearch={onSearch} language={language} /><button className="language-toggle" onClick={onToggleLanguage} aria-label={nepali ? 'Switch to English' : 'नेपालीमा बदल्नुहोस्'}><Languages size={15} /> {nepali ? 'English' : 'नेपाली'}</button><div className="nav-status"><span className="live-dot" /> {nepali ? 'प्रत्यक्ष निगरानी' : 'Live monitoring'} <i /> <Radio size={15} /> {nepali ? 'निःशुल्क' : 'Free to use'} <button className="theme-toggle" onClick={onToggleTheme} aria-label={`Switch to ${theme === 'light' ? 'dark' : 'light'} mode`}>{theme === 'light' ? <Moon size={16} /> : <Sun size={16} />}</button>{onEmployeeDashboard && <button className="employee-nav-button" onClick={onEmployeeDashboard} aria-label={nepali ? 'कर्मचारी ड्यासबोर्ड खोल्नुहोस्' : 'Open employee dashboard'}><LayoutDashboard size={15} /> {nepali ? 'ड्यासबोर्ड' : 'Dashboard'}</button>}<button className="signout-button" onClick={onSignOut} aria-label={nepali ? 'साइन आउट' : 'Sign out'}><LogOut size={15} /> {nepali ? 'साइन आउट' : 'Sign out'}</button></div></header> }
